CIDA
====

To run cida, simply execute the shell script:

chmod +x runcida.sh
./runcida.sh

Optionally, pass the directory containing the data to analyse as an argument:

./runcida.sh ~/data/directory
